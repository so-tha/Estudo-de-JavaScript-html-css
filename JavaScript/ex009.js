var vel = 60.5
console.log(`A velocidade do seu carro é ${vel}`)
     if(vel>60){ //condição simples, pois não possui senão(else)
      console.log(`Voce ultrapassou a velocidae permitida`)   
     }

